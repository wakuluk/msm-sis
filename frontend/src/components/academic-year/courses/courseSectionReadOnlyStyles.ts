// Shared Mantine style overrides for readable disabled course-section fields.
export const readableDisabledInputStyles = {
  input: {
    backgroundColor: 'light-dark(var(--mantine-color-gray-0), var(--mantine-color-dark-6))',
    color: 'var(--mantine-color-text)',
    opacity: 1,
  },
  label: {
    color: 'var(--mantine-color-text)',
    opacity: 1,
  },
};

export const readableDisabledSwitchStyles = {
  label: {
    color: 'var(--mantine-color-text)',
    opacity: 1,
  },
  description: {
    opacity: 1,
  },
  track: {
    opacity: 1,
  },
};

export const readableDisabledCheckboxStyles = {
  input: {
    opacity: 1,
  },
  label: {
    color: 'var(--mantine-color-text)',
    opacity: 1,
  },
};
