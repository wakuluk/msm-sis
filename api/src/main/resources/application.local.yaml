spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/msmsisdb
    username: root
    password: msmsisdb
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/testdata

app:
  security:
    jwt:
      secret: change-me-to-a-long-random-development-secret-at-least-32-bytes
      expiration-seconds: 3600
  storage:
    root-dir: ./uploads
