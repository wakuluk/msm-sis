spring:
  application:
    name: api

  datasource:
    url: jdbc:postgresql://msm-sis-db:5432/msmsisdb
    username: root
    password: msmsisdb
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
app:
  cors:
    allowed-origin: http://localhost:5173
  security:
    jwt:
      secret: ${APP_JWT_SECRET:change-me-to-a-long-random-development-secret-at-least-32-bytes}
      expiration-seconds: ${APP_JWT_EXPIRATION_SECONDS:3600}
  storage:
    root-dir: ${APP_STORAGE_ROOT_DIR:./uploads}

server:
  port: 8080
